# Docker assets

Application Dockerfiles may live here later.

**PI-005:** local development uses root `docker-compose.yml` for **PostgreSQL + Redis only**. Frontend, backend, and agent are not containerized yet.
