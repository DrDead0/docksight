# DockSight Server

NestJS modular monolith backend for DockSight.

## Scripts

```bash
npm run start:dev      # watch mode
npm run build          # compile
npm run prisma:generate
npm run prisma:migrate
```

API prefix: `/api`  
Swagger: `/docs`  
Health: `/api/health`
